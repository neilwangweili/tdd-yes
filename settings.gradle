rootProject.name = 'tdd-yes'

file("modules").eachDir {
    include it.name
    project(":${it.name}").projectDir = it
}

file("frameworks").eachDir {
    include it.name
    project(":${it.name}").projectDir = it
}
